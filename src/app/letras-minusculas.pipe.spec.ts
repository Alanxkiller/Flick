import { LetrasMinusculasPipe } from './letras-minusculas.pipe';

describe('LetrasMinusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new LetrasMinusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
