import { LetrasMayusculasPipe } from './letras-mayusculas.pipe';

describe('LetrasMayusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new LetrasMayusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
